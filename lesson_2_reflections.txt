What happens when you initialize a repository? Why do you need to do it?
->git creates a folder .git in the directory. we need to do it to keep track of the further changes that will be made to the files in the directory

How is the staging area different from the working directory and the repository? What value do you think it offers?
-> Staging area provides more control on git commit operations. It decides which files will get tracked